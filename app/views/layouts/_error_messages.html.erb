<% if book.errors.any? %>
  <div class="alert alert-warning">
  	<%= book.errors.count.to_s+' errors prohibited this obj from being saved:' %>
    <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>